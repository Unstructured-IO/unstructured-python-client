"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""
# @generated-id: 6bfbd0ec296c

from __future__ import annotations
from datetime import datetime
from pydantic import model_serializer
from typing import List, Optional
from typing_extensions import NotRequired, TypedDict
from unstructured_client.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)


class MarkReadRequestTypedDict(TypedDict):
    r"""Request to mark notifications as read.

    Provide ONE of: notification_ids, before, or mark_all (mutually exclusive).
    workflow_id is valid with 'before' or 'mark_all'.
    """

    before: NotRequired[Nullable[datetime]]
    r"""Mark all notifications created before this ISO 8601 timestamp as read"""
    mark_all: NotRequired[bool]
    r"""Mark all unread notifications as read (can combine with workflow_id)"""
    notification_ids: NotRequired[Nullable[List[str]]]
    r"""Specific notification IDs to mark as read (max 100)"""
    workflow_id: NotRequired[Nullable[str]]
    r"""Filter by workflow (only valid with 'before' or 'mark_all')"""


class MarkReadRequest(BaseModel):
    r"""Request to mark notifications as read.

    Provide ONE of: notification_ids, before, or mark_all (mutually exclusive).
    workflow_id is valid with 'before' or 'mark_all'.
    """

    before: OptionalNullable[datetime] = UNSET
    r"""Mark all notifications created before this ISO 8601 timestamp as read"""

    mark_all: Optional[bool] = False
    r"""Mark all unread notifications as read (can combine with workflow_id)"""

    notification_ids: OptionalNullable[List[str]] = UNSET
    r"""Specific notification IDs to mark as read (max 100)"""

    workflow_id: OptionalNullable[str] = UNSET
    r"""Filter by workflow (only valid with 'before' or 'mark_all')"""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["before", "mark_all", "notification_ids", "workflow_id"])
        nullable_fields = set(["before", "notification_ids", "workflow_id"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m
