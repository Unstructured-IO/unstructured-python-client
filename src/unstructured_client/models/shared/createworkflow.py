"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from enum import Enum
from typing_extensions import TypedDict
from unstructured_client.types import BaseModel


class CreateWorkflowSchedule(str, Enum):
    EVERY_15_MINUTES = "every 15 minutes"
    EVERY_HOUR = "every hour"
    EVERY_2_HOURS = "every 2 hours"
    EVERY_4_HOURS = "every 4 hours"
    EVERY_6_HOURS = "every 6 hours"
    EVERY_8_HOURS = "every 8 hours"
    EVERY_10_HOURS = "every 10 hours"
    EVERY_12_HOURS = "every 12 hours"
    DAILY = "daily"
    WEEKLY = "weekly"
    MONTHLY = "monthly"


class CreateWorkflowWorkflowType(str, Enum):
    BASIC = "basic"
    ADVANCED = "advanced"
    PLATINUM = "platinum"


class CreateWorkflowTypedDict(TypedDict):
    destination_id: str
    name: str
    schedule: CreateWorkflowSchedule
    source_id: str
    workflow_type: CreateWorkflowWorkflowType


class CreateWorkflow(BaseModel):
    destination_id: str

    name: str

    schedule: CreateWorkflowSchedule

    source_id: str

    workflow_type: CreateWorkflowWorkflowType
