"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict
from unstructured_client.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)


class KafkaCloudSourceConnectorConfigInputTypedDict(TypedDict):
    bootstrap_servers: str
    kafka_api_key: str
    secret: str
    topic: str
    group_id: NotRequired[Nullable[str]]
    num_messages_to_consume: NotRequired[int]
    port: NotRequired[int]


class KafkaCloudSourceConnectorConfigInput(BaseModel):
    bootstrap_servers: str

    kafka_api_key: str

    secret: str

    topic: str

    group_id: OptionalNullable[str] = UNSET

    num_messages_to_consume: Optional[int] = 100

    port: Optional[int] = 9092

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = ["group_id", "num_messages_to_consume", "port"]
        nullable_fields = ["group_id"]
        null_default_fields = []

        serialized = handler(self)

        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)
            serialized.pop(k, None)

            optional_nullable = k in optional_fields and k in nullable_fields
            is_set = (
                self.__pydantic_fields_set__.intersection({n})
                or k in null_default_fields
            )  # pylint: disable=no-member

            if val is not None and val != UNSET_SENTINEL:
                m[k] = val
            elif val != UNSET_SENTINEL and (
                not k in optional_fields or (optional_nullable and is_set)
            ):
                m[k] = val

        return m
